# Set default configuration
spring:
  main:
    web-application-type: reactive
management:
  endpoints:
    web:
      exposure:
        include: health
springdoc:
  api-docs:
    enabled: true
    path: "/api/openapi"
  swagger-ui:
    enabled: false
singularity:
  content:
    articles:
      enable: true